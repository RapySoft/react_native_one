import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

export const Tab3Screen = () => {
  return (
    <View style={styles.container}>
      <Text>screen tab 3</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
});
